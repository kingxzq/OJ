class MyHashSet {
    unordered_set<int> hash;
public:
    MyHashSet() {

    }
    
    void add(int key) {
        hash.insert(key);
    }
    
    void remove(int key) {
        hash.erase(key);
    }
    
    bool contains(int key) {
        return hash.count(key);
    }
};
